spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:${LOCAL_MYSQL_PORT}}/${MYSQL_DATABASE:${LOCAL_MYSQL_DATABASE}}
    username: ${MYSQL_USER:${LOCAL_MYSQL_USER}}
    password: ${MYSQL_PASSWORD:${LOCAL_MYSQL_PASSWORD}}
    driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    jdbc:
      initialize-schema: ALWAYS
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        generate_statistics: false
        jdbc:
          batch_size: 20
      order_inserts: true
      order_updates: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

  main:
    allow-bean-definition-overriding: true
  http:
    codecs:
      max-in-memory-size: 20MB

aws:
  region: ${S3_REGION}
  s3:
    buckets:
      db-images: ${S3_BUCKET}
      root: ${S3_BUCKET_ROOT}
  cloudfront:
    url: https://cdn.moonkeyeu.com/

application:
  backend:
    url: http://localhost:8080/api/v1/
  path:
    root:
      data:
        folder:
          csv: ${user.home}/Desktop/Projects/MoonkeyEU/MoonkeyEU-Github/backend/data/csv-dataset
          json: ${user.home}/Desktop/Projects/MoonkeyEU/MoonkeyEU-Github/backend/data/json-dataset
          images: ${user.home}/Documents/moonkeyeu_db_images
  api:
    the-space-devs:
      url: https://ll.thespacedevs.com
      version: 2.3.0

management:
  endpoints:
    web:
      exposure:
        include: [ "health", "prometheus", "metrics" ]
  endpoint:
    health:
      show-details: always
      group:
        core-api:
          include: db
  health:
    db:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

server:
  port: 8081
  servlet:
    context-path: /api/v1/

logging:
  level:
    security:
      web: DEBUG
    org:
      springframework:
        jdbc:
          datasource:
            DriverManagerDataSource: DEBUG
      hibernate:
        SQL: off
        type:
          descriptor:
            sql:
              BasicBinder: off

