prometheus.scrape "actuator_metrics" {
  targets = [
    {"__address__" = "moonkeyeu-core-api:8080"},
    {"__address__" = "moonkeyeu-etl-api:8081"},
  ]
  metrics_path = "/api/v1/actuator/prometheus"
  scrape_interval = "15s"
  forward_to = [prometheus.remote_write.default.receiver]
}
