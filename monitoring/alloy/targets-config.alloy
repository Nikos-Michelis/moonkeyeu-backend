// Prometheus Metrics Collector
prometheus.remote_write "default" {
  endpoint {
    url = "http://prometheus-container:9090/api/v1/write"
  }
}

// Loki Logs Collector
loki.write "default" {
    endpoint {
        url = "http://loki-container:3100/loki/api/v1/push"
    }
}